export function template(state=false,action){
    switch (action.type){
        case `[${TEMPLATE}:${GET}]`:
        case `[${TEMPLATE}:${POST}]`:
        case `[${TEMPLATE}:${PATCH}]`:        
    }
}